/**
 * src/farewell4.js — minimal farewell module.
 *
 * Exports:
 *   farewell(name) → string  — returns 'goodbye, ' followed by name
 */

export function farewell(name) {
  return 'goodbye, ' + name;
}
